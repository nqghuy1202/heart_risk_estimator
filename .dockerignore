# Git / IDE
.git
.gitignore
.vscode
.idea
.claude
.agents

# Python
.venv
venv
__pycache__
*.py[cod]
.pytest_cache

# Frontend build artifacts & deps (rebuilt inside the image)
frontend/node_modules
frontend/dist

# Env & secrets
.env
.env.prod

# Notebook / training data / dev DB (not needed to serve the app — the trained
# adaboost.joblib is what matters, and migrate creates a fresh db.sqlite3 in the image)
*.ipynb
backend/heart.csv
backend/db.sqlite3
img/
docs/
*.md

# Vercel
.vercel
